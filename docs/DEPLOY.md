# DEPLOY.md

## Назначение

Этот файл описывает, как переносить изменения из DEV в PROD безопасно и предсказуемо.

Цель:
- не ломать прод
- не терять пользователей
- не получать "всё работало в DEV, но не работает в PROD"

---

## 1. Общий принцип

Работаем строго по цепочке:

```text
DEV → проверка → миграции → деплой → проверка PROD

